This script compares official HSK 3.0 vocabulary entries against the classic in-repo HSK 1-6
word list using an exact `hanzi + pinyin` match. It helps estimate how much content can be
reused before generating new meanings, sentences, or audio mappings.
